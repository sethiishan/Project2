 <section id="login-signup">
     <div class="tab-content-parent tab-content">
        <div id="student_form" class="form">
      <ul class="tab-group">
        <li class="tab st-tab active"><a href="#st-signup">Sign Up</a></li>
        <li class="tab st-tab"><a href="#st-login">Log In</a></li>
      </ul>
      <div class="tab-content-student tab-content">
        <div id="st-signup">   
          <h1>Sign Up</h1>
          <% if(message.length>0) {%>
            <div class="alert alert-danger"><%= message %></div>
          <% } %>
          <form id="form-signup" method="post" action="/signup">
          <div class="field-wrap">
            <label>
              Email Address<span class="req">*</span>
            </label>
            <input name="email" type="email"required autocomplete="off"/>
          </div>
          <% if(otp) {%>
          <div class="top-row">
            <div class="field-wrap">
              <label>
                First Name<span class="req">*</span>
              </label>
              <input type="text" name="first_name" required autocomplete="off" />
            </div>
        
            <div class="field-wrap">
              <label>
                Last Name<span class="req">*</span>
              </label>
              <input type="text" name="last_name" required autocomplete="off"/>
            </div>

            <div class="field-wrap">
              <label>
                College/Company<span class="req">*</span>
              </label>
              <input type="text" name="institution" required autocomplete="off"/>
            </div>
            <div class="field-wrap">
              <label>
                phone number<span class=""></span>
              </label>
              <input type="tel" name="phone" autocomplete="off"/>
            </div>

          </div>

          
          
          <div class="field-wrap">
            <label>
              Set A Password<span class="req">*</span>
            </label>
            <input name="password" type="password" required autocomplete="off"/>
          </div>
          

          <div class="field-wrap" id="otp" style="display: block;">
            <label>
              OTP <span class="req">*</span>
            </label>
            <input name="otp" type="text"required autocomplete="off"/>          
          </div>
          <% } %>

          <span class="agreement">By clicking Join now, you agree to the User Agreement, Privacy Policy, and Cookie Policy.</span>
          <button type="submit"  class="button button-block">Get Started</button>
          
          </form>

        </div>
        
        <div id="st-login">   
          <h1>Welcome Back!</h1>
          <% if(message.length>0) {%>
            <div class="alert alert-danger"><%= message %></div>
          <% } %>
          <form action="/login" method="post">
          
            <div class="field-wrap">
            <label>
              Email Address<span class="req">*</span>
            </label>
            <input name="email" type="email"required autocomplete="off"/>
          </div>
          
          <div class="field-wrap">
            <label>
              Password<span class="req">*</span>
            </label>
            <input name="password" type="password"required autocomplete="off"/>
          </div>
          
          <p class="forgot"><a href="/forget">Forgot Password?</a></p>
          <button class="button button-block">Log In</button>
          
          </form>

        </div>
        
      </div><!-- tab-content -->
    </div>
    <div id="company_form" class="form">
  <ul class="tab-group">
    <li class="tab cp-tab active"><a href="#cp-signup">Sign Up</a></li>
    <li class="tab cp-tab"><a href="#cp-login">Log In</a></li>
  </ul>
  <div class="tab-content-company tab-content">
    <div id="cp-signup">   
      <h1>Sign Up</h1>
      <% if(message.length>0) {%>
            <div class="alert alert-danger"><%= message %></div>
          <% } %>
      <form action="/signup1" method="post">
      <div class="top-row">
        <div class="field-wrap">
          <label>
            Company Name<span class="req">*</span>
          </label>
          <input name="company_name" type="text" required autocomplete="off" />
        </div>
<!--
        
            <div class="field-wrap">
              <label>
                Last Name<span class="req">*</span>
              </label>
              <input type="text"required autocomplete="off"/>
            </div>
-->
          </div>

          <div class="field-wrap">
            <label>
              Email Address<span class="req">*</span>
            </label>
            <input name="email" type="email"required autocomplete="off"/>
          </div>
          
          <div class="field-wrap">
            <label>
              Set A Password<span class="req">*</span>
            </label>
            <input name="password" type="password"required autocomplete="off"/>
          </div>
          <span class="agreement">By clicking Join now, you agree to the  User Agreement, Privacy Policy, and Cookie Policy.</span>
          <button type="submit" class="button button-block">Get Started</button>
          
          </form>

        </div>
        
        <div id="cp-login">   
          <h1>Welcome Back!</h1>
          <% if(message.length>0) {%>
            <div class="alert alert-danger"><%= message %></div>
          <% } %>
          <form action="/login" method="post">
          
            <div class="field-wrap">
            <label>
              Email Address<span class="req">*</span>
            </label>
            <input name="email" type="email"required autocomplete="off"/>
          </div>
          
          <div class="field-wrap">
            <label>
              Password<span class="req">*</span>
            </label>
            <input name="password" type="password"required autocomplete="off"/>
          </div>
          
          <p class="forgot"><a href="#">Forgot Password?</a></p>
          <button class="button button-block">Log In</button>
          
          </form>

        </div>
        
      </div><!-- tab-content -->
    </div> <!-- form --> 
     </div>
 </section>